import 'package:flutter/material.dart';

class SettingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: _mainBody(),
    );
  }

  _mainBody() {
    return Container(child: Text("Main bodyy"),);
  }
}
